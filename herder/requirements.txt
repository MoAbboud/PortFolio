# Stage 0 - the scaffold. Nothing here is a model.
#
# The local models arrive with the stages that need them, so an early stage is never
# blocked on a multi-gigabyte download. See requirements/05-tasks.md.

fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30
alembic>=1.14
pgvector>=0.3.6
pydantic>=2.10
pydantic-settings>=2.7
pytest>=8.3
pytest-asyncio>=0.25
httpx>=0.28

# Stage 8 - the server-rendered pages. Already present as a transitive dependency, which is
# not the same as being declared: stated here so a change upstream cannot remove it silently,
# the way sentence-transformers was missing for five stages.
jinja2>=3.1

# Stage 1 - counting tokens. The budget ruler, documented as an estimator.
tiktoken>=0.8

# Stage 2 - the local extractor. A few gigabytes of weights, fetched on purpose
# with `python -m herder models pull --llm`, never as a side effect of an install.
# llama-cpp-python>=0.3

# Stage 3 - the NLI model that decides merge verdicts, and at stage 6 grades probes.
# Embeddings moved to Ollama (all-minilm), so this library is only for NLI.
#
# **Was commented out until the stage 6 review, and that was a bug.** Stage 3 installed it
# into the local venv by hand and never uncommented it here, so a fresh clone - and the
# Docker image - could not run a derive or a checkpoint at all: the worker failed every
# checkpoint job with "sentence-transformers is not installed". The CLI worked only because
# the venv had it. The library installs with pip; the NLI weights (~700 MB) still download
# on first use, not at install.
#
# It pulls in torch. From PyPI on Linux that is the CUDA build, several gigabytes, which a
# CPU-only Docker image does not need - see the note in the Dockerfile.
sentence-transformers>=6.0
