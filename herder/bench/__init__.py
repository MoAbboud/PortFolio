"""The stage 9 benchmark: a corpus, hand-written fact lists, and a harness that compares methods.

Outside the application on purpose. It drives the real pipeline through the API rather than
reimplementing any of it - a harness that reimplements extraction measures the reimplementation.
"""
