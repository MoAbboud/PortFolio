# The Expo SDK 54 / React 19 ecosystem still has some loose peer ranges, which
# trip npm's strict resolver. Allow installs to proceed (Expo pins compatible
# versions anyway via `expo install`).
legacy-peer-deps=true
