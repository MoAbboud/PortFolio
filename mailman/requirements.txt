# Stage 0 - scaffold only. pdfplumber and the provider SDK arrive with the stages
# that need them, so an early stage is never blocked on a dependency it does not use.
fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0.36
alembic>=1.14
psycopg[binary]>=3.2
pydantic>=2.10
pydantic-settings>=2.7
python-multipart>=0.0.20
pytest>=8.3
httpx>=0.28
