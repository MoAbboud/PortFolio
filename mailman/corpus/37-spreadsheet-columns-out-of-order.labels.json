{
  "name": "37-spreadsheet-columns-out-of-order",
  "tests": "A CSV with the columns in an order nothing expects. The pipeline ingests PDFs only, so this is refused at the door rather than misread.",
  "should_fail": false,
  "unsupported": "spreadsheet ingestion is not built; the media check refuses it",
  "expected": {}
}