# Single LLM provider to start: Anthropic.
# The SDK gives us exponential-backoff retries on 429 / 5xx out of the box.
anthropic>=0.40

# Web interface (`python -m fallacy_warn serve`).
flask>=3.1.3

# Local TF-IDF model backend (lightweight; deployable on a free tier — no torch).
scikit-learn>=1.3
joblib>=1.3

# The trained DistilBERT stages (models/v2_bert and later). The app auto-detects the
# model sets under models/ and prefers them; override with FALLACY_BACKEND.
# On a CPU-only machine install torch from the CPU index first (the Dockerfile does):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0
transformers>=4.30

# Production web server, used by deploy/docker-compose.prod.yml. `serve` is Flask's
# development server and is only for local use.
gunicorn>=22.0
