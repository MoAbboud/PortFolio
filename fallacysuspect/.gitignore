# Secrets — never commit the API key
.env
*.env
.env.*

# SQLite database (local evaluations) + WAL sidecar files
*.db
*.db-wal
*.db-shm

# Large trained model weights — keep out of git (host separately for deploy).
# The small, deployable TF-IDF models + pipeline.json under models/ DO commit.
*.safetensors
*.pt
**/*_distilbert/

# Datasets (licensed — download separately)
data/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
